// Online C compiler to run C program online
#include <stdio.h>
int fact(int n)
{
    if(n==1)
    return 1;
    else
    return n*fact(n-1);
}
main()
{
    int n;
    scanf("%d",&n);
    int r=fact(n);
    printf("%d",r);
}
